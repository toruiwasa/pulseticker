import { Stack } from 'expo-router';

/**
 * Placeholder root layout. Task 10 (#13) replaces this with the real auth
 * guard, the (auth)/(tabs) group split and the OAuth deep-link handler.
 */
export default function RootLayout() {
  return <Stack />;
}
